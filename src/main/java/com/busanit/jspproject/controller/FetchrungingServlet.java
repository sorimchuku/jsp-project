package com.busanit.jspproject.controller;

import javax.servlet.*;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.*;
import java.io.IOException;
import java.io.PrintWriter;

// Example servlet to fetch running data in JSON format
//@WebServlet("/fetchRunningData")
//public class FetchRunningDataServlet extends HttpServlet {
   // protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
      //  response.setContentType("application/json");
      //  PrintWriter out = response.getWriter();

        // Fetch data from the database
        //List<RunLog> runLogs = yourDAO.getRunningLogs();

        // Convert to JSON format (you can use Gson or any other JSON library)
       // String json = new Gson().toJson(runLogs);
       // out.write(json);
     //   out.flush();
 //   }
//}

